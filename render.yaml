services:
  - type: web
    name: LaylaBot
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn layla_bot:app"
    envVars:
      - key: OPENAI_API_KEY
        sync: false
